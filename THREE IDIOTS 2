#include <stdio.h>

int main() {
    
    int x1 = 6, y1 = 5;
    int x2 = 3, y2 = 8;
    float mid_x = (x1 + x2) / 2.0;
    float mid_y = (y1 + y2) / 2.0;


    printf("Binoy's house is located at (%.1f,%.1f)\n", mid_x, mid_y);

    return 0;
}
